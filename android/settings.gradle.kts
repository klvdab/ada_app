// 협회 안드로이드 앱 — 설정 (1.0.0판, 빌드 260923-1)
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "AdaApp"
include(":app")
